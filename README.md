# Lab 5 - Starter
Make sure you make a PR to your own repo's main and not the class' repo!! Otherwise you will lose points!!  
Name: Bethany Miyamoto  

1. Would you use a unit test to test the “message” feature of a messaging application? Why or why not? For this question, assume the “message” feature allows a user to write and send a message to another user. **Answer:** No, I would not. Sending a message would require multiple machines/systems and a network connection. Unit tests are just designed to test small features of functionality. 

2. Would you use a unit test to test the “max message length” feature of a messaging application? Why or why not? For this question, assume the “max message length” feature prevents the user from typing more than 80 characters. **Answer:** Yes, I would use a unit test to test this feature. This is a small, easy piece of logic that we can check. Given an input string, does it or does it not exceed 80 characters?
